external-dns:
  global:
    imageRegistry: "registry.durp.info"

  image:
    pullPolicy: Always

  sources:
    - ingress
    
  provider: cloudflare
  cloudflare:
    secretName : "external-dns"
    proxied: true