apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-configmap
data:
  traefik.yaml: |
    http:
      services:
        authentik-service:
          loadBalancer:
            servers:
            - url: https://google.com
            passHostHeader: true
