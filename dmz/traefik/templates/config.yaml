apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-configmap
data:
  traefik.yaml: |
    http:
      routers:
        router0:
          entryPoints:
          - web
          service: service-foo
          rule: host(`test.durp.info`)

      services:
        service-foo:
          loadBalancer:
            servers:
            - url: https://google.com
            passHostHeader: true
