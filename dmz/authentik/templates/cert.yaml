---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-outpost-tls
spec:
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  secretName: authentik-outpost-tls
  commonName: "*"
  dnsNames:
    - "*"
