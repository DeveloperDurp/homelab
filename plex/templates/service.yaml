apiVersion: v1
kind: Service
metadata:  
  name: plex
spec:
  selector:    
    component: deployment
  type: ClusterIP
  ports:  
  - name: https
    port: 32400
    targetPort: 32400
    protocol: TCP